/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    output: 'standalone', // Asegura que Next.js sea 'standalone' para entornos como Vercel
    };
    
    export default nextConfig;
